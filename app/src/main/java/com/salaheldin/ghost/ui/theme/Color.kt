package com.salaheldin.ghost.ui.theme

import androidx.compose.ui.graphics.Color

// Backgrounds — tinted, not pure white/black
val LightBackground = Color(0xFFF8F7FC)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFF0EEF9)
val LightOnBackground = Color(0xFF1C1B1F)
val LightOnSurfaceVariant = Color(0xFF6F6C75)
val LightOutline = Color(0xFFDAD6E8)

// Dark — soft charcoal, not pure black
val DarkBackground = Color(0xFF0F0F12)
val DarkSurface = Color(0xFF18181D)
val DarkSurfaceVariant = Color(0xFF202027)
val DarkOnBackground = Color(0xFFEDEDF0)
val DarkOnSurfaceVariant = Color(0xFFA3A1AB)
val DarkOutline = Color(0xFF303038)

// Ghost purple/lavender accent
val AccentLight = Color(0xFF7C5CFF)
val AccentDark = Color(0xFF8B7CF6)

// Priority / risk signal colors — reserved ONLY for Ghost's attention signals
val PriorityHigh = Color(0xFFEF5350)
val PriorityMedium = Color(0xFFFFB74D)
val PriorityLow = Color(0xFF66BB6A)